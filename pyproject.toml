[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cnn-thesis"
version = "0.0.0"
description = "Skeleton repo for CNN thesis work"
authors = [ { name = "Your Name" } ]

[tool.poetry.dev-dependencies]
pytest = "^7.0"


